import React from "react";
import classes from "styles/components/MainHeader.module.css";

const MainHeader = () => {
  return (
    <header className={classes.header}>
      <span className={classes.logo42seoul}>42 logo</span>
    </header>
  );
};

export default MainHeader;
